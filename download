# ─── PUBLIC (visible in browser) ─────────────────────────────────────────────
# These get embedded in the frontend JS bundle

NEXT_PUBLIC_HUB_NAME="Script Loader"
NEXT_PUBLIC_HUB_SUB="Script Loader"
NEXT_PUBLIC_HUB_EMOJI="⚡"

# IMPORTANT: set this to your full Vercel URL so the loadstring is correct
NEXT_PUBLIC_LOADER_URL="https://your-project.vercel.app/api/loader"

NEXT_PUBLIC_WEBSITE_URL="https://yourwebsite.com"
NEXT_PUBLIC_DISCORD_URL="https://discord.gg/yourserver"

# ─── PRIVATE (server-side only) ────────────────────────────────────────────────
# Never exposed to the browser

# Optional: require ?key=VALUE on every /api/loader request
# Leave blank = no key required (open access)
LOADER_KEY=""

# Script version shown in /api/status
LOADER_VERSION="1.0.0"
LOADER_NAME="Script Loader"

# ─── SCRIPT CONTENT (choose one method) ───────────────────────────────────────
#
# METHOD A — Env var (recommended for Vercel)
#   Paste the full obfuscated Lua script here.
#   WARNING: Vercel env vars max ~4KB visible in dashboard.
#   For large scripts (like g.lua at 248KB), use METHOD B.
#
SCRIPT_CONTENT=""

# METHOD B — data/script.lua file (bundled in deployment)
#   Place your obfuscated .lua file at:  data/script.lua
#   The API reads it automatically when SCRIPT_CONTENT is empty.
#   This file is NOT served publicly — only the API can read it.
#   For the g.lua file (248KB), use this method.
